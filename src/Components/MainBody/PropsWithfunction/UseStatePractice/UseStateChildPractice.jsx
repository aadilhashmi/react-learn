import React from "react";

const UseStateChildPractice = (item) => {
  return (
    <div>
      <p>
        i am {item.name} and age is {item.age}
      </p>
    </div>
  );
};

export default UseStateChildPractice;
