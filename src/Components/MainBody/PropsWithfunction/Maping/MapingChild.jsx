import React from "react";

const MapingChild = (item) => {
  return (
    <div>
      <p>
        My name {item.name} and my age {item.age}
      </p>
    </div>
  );
};

export default MapingChild;
